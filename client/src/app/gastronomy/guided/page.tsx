"use client";

export default function GastronomyGuided() {
  return (
    <main className="min-h-screen bg-[#fbfbfc] text-[#5c5d60] py-20 px-8 max-w-4xl mx-auto font-mono text-xs leading-relaxed">
      <div className="border-b border-[#19191b]/10 pb-6 mb-10">
        <span className="text-[#19191b]">// PLATFORM BLUEPRINTS</span>
        <h1 className="text-3xl font-serif text-[#19191b] mt-1 uppercase">
          Site #9 Minimal Culinary System
        </h1>
      </div>

      <div className="space-y-12">
        <section>
          <h3 className="text-sm font-bold text-[#19191b] mb-2">
            1. Fluid Ambient Porcelain Shadow Concept
          </h3>
          <p>
            Unlike dark canvas trackers, this layout injects coordinate
            calculations directly into the layout background template container
            using a responsive CSS{" "}
            <code className="bg-[#edeef2] text-[#19191b] px-1 rounded">
              radial-gradient
            </code>{" "}
            parameter matrix. This produces an organic lighting illusion that
            updates fluidly on every cursor movement loop.
          </p>
        </section>

        <section className="bg-[#19191b] p-6 text-[#fbfbfc] rounded-sm">
          <h4 className="font-bold mb-2">Replication Prompt Code</h4>
          <pre className="text-white/80 whitespace-pre-wrap text-[11px]">
            {`Build an ultra-premium stark minimalist lighter framework landing grid in Next.js. Deploy an elegant text-morph matrix that updates layout weights dynamically on mouse interaction, coupled to an asset wrapper running real-time responsive inline background gradients matching client cursor paths.`}
          </pre>
        </section>
      </div>
    </main>
  );
}
